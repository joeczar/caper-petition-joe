<div class="formWrapper shadow-3">
        <header class="formHeader shadow-3">
        <h2>Login</h2>
    </header>
        <form class="form" id="login" name="login" action='/login' method='POST'>
            {{>error}}
            {{>emailInput id='emailInput' label='E-Mail' placeholder='stan@getz.org'}}
            {{>passwordInput id='pwdInput' label='Password' placeholder='Your password'}}
            {{>submitBtn id='submit' text="Register"}}
            {{>csrf}}
        </form>
    </div>
